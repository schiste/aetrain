# Web App Shell

This folder is for browser startup and top-level composition.

Expected responsibilities:

- initializing the app
- wiring together state, data loading, worker boot, and renderer setup
- choosing between debug and production runtime modes

Heavy logic should not accumulate here.
