# Web Public Assets

This folder is for browser-served static assets.

Keep here:

- generated runtime data under `data/`
- images, icons, and other static resources when they exist

Do not place source-of-truth build artifacts here unless they are explicitly
meant to ship to the browser.
