# Android Bridge

This folder is for Android bindings to the shared Rust core.

Expected responsibilities:

- JNI or generated bindings
- typed adapters between Kotlin models and Rust results
- worker or coroutine integration for background computations
