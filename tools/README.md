# Tools

Operator-facing utilities and local developer tools belong here.

Examples:

- pipeline orchestration
- import helpers
- one-off repo utilities that should still be tracked and documented

Rules:

- reusable domain logic stays in `packages/rust/`
- this folder should stay thin and task-oriented
