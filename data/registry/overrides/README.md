# Registry Overrides

Registry overrides are allowed, but they are the exception path.

This directory owns:

- deterministic name rules
- country inference exceptions
- rare explicit registry overrides with rationale

Rules should be preferred over one-off overrides whenever possible.
