# Wikidata Raw Snapshots

Seed and incremental raw Wikidata registry snapshots will live here.

Expected future contents:

- dump-derived seed extracts
- recent-change refresh batches
- source metadata per snapshot
