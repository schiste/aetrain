# Raw Registry Snapshots

This directory will hold immutable raw seed and refresh snapshots for the
registry sources.

Planned source groups:

- `wikidata/`
- `osm/`

These snapshots are source-native and should not be edited in place.
