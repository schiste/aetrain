# Registry State

This directory stores operational state for the registry pipeline.

Subdirectories:

- `cursors/`: last applied recent-change or replication positions
- `snapshots/`: seed-complete markers and snapshot manifests
