# Manual Overrides

Manual curation is the exception path.

Use an override only when deterministic normalization logic cannot resolve the
problem with acceptable confidence.

Every override must record:

- what source entities are affected
- what canonical city they map to
- why code-based normalization was insufficient
- who added it
- when it was added
- where the decision is tracked
