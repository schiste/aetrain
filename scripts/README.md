# Compatibility Scripts

This folder is for stable shim entrypoints and compatibility wrappers.

Use it when a tool moves but a documented command should keep working.

Heavy logic belongs in:

- `tools/` for operator-facing utilities
- `packages/rust/` for reusable implementation code
