[package]
name = "aetrain-dataset"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
aetrain-domain = { path = "../aetrain-domain" }
serde = { version = "1.0", features = ["derive"] }
