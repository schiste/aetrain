# TypeScript Browser Glue

This namespace exists because browsers require JavaScript-compatible code.

Its role is intentionally narrow:

- wasm bindings
- worker wrappers
- browser-safe adapters
- small convenience layers for the web app

It is not the place for the shared routing or product engine.
